---
sidebar_position: 4
---

import { ElementsTable } from '@site/src/components/ElementsTable';

# Elements

Overview of all frame elements.

<ElementsTable />

All string fields are null-terminated, except `Text` and DTYPE `0x0A` signal values, which are
length-prefixed.
